

This is the Memory Mapped assignment for COM S 352, 

using mutual exclusion. 

In order to compile the project, open up terminal or command line using Pyrite and run "make alloc". 

To run the file, do "./alloc"

To test prov-rep, open up another terminal or command line using Pyrite and run "make prov-rep".

To run the file, do "./prov-rep".
